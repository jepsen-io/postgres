# We add these to the postgres conf.d directory.

# Listen on the network.
listen_addresses = '*'

log_statement=all
# We'd like to test this frequently
autovacuum_naptime = 5s

# We love connecting to databases
max_connections = 4098
